<html>
  <head>
    <title>{{ subject }}</title>
    <style>
        h2{
            font-family: Arial, Helvetica, sans-serif;
            text-decoration: underline;
        }
        h4 {
            font-size: 1.2rem;
            margin: 0;
            font-family: Arial, Helvetica, sans-serif;
        }
        p {
            font-family: Arial, Helvetica, sans-serif;
            margin-bottom: 1rem;
            margin-top: 0;
        }
        a {
            font-family: Arial, Helvetica, sans-serif;
            margin-top: 1rem;
        }
    </style>
  </head>
  <body>
    <h2{{ subject }}</h2>
    <h4>Hai {{ name }}.</h4>
    <p>{{ message }}</p>
    <a href="{{ link }}">{{ button }}</a>
  </body>
</html>